import { createMuiTheme } from '@material-ui/core/styles'

const palette = {
    primary: { main: '#4A148C' },
    secondary: { main: '#B2EBF2' }
}
const themeName = 'Persian Indigo Blizzard Blue Bearded Dragon'

export default createMuiTheme({ palette, themeName })